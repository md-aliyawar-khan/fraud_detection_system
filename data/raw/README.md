# Raw data

Place `creditcard_2023.csv` in this folder before running the training pipeline.

The file is not committed to GitHub because it is larger than GitHub's 100 MB file limit (~325 MB).

After adding the dataset:

```bash
python scripts/preprocess.py
python scripts/train.py
```
